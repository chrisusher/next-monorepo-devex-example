{
  "name": "@repo/domain",
  "version": "0.0.0",
  "private": true,
  "exports": {
    "./projects/permissions": "./src/projects/permissions.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run"
  },
  "dependencies": {
    "@repo/api-contracts": "workspace:*"
  },
  "devDependencies": {
    "@repo/config": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
